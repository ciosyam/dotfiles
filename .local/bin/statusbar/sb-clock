#!/bin/sh

clock=$(date '+%I')

case $BLOCK_BUTTON in
	1) notify-send "This Month" "$(cal | sed "s/\<$(date +'%e')\>/<b><span color='red'>&<\/span><\/b>/")" && notify-send "Get Things Done" "$(calcurse -t)" ;;
	2) setsid -f "$TERMINAL" -e calcurse ;;
esac

date "+(%a) %Y/%m/%d $icon%I:%M%p"
